# V2 Local Server

Run the v2 sandbox from the repo root:

```bash
node v2/server.mjs
```

Then open:

```text
http://127.0.0.1:8020/
```

To use a different port:

```bash
PORT=8021 node v2/server.mjs
```
